apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: productpage-dev
spec:
  port:
    targetPort: 8080-tcp
  to:
    kind: VirtualService
    name: productpage-dev
  tls:
    termination: edge
