apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: productpage-1.1-SNAPSHOT
spec:
  hosts:
    - productpage
  http:
    - route:
        - destination:
            host: productpage
            subset: 1.1-SNAPSHOT
          weight: "45"
        - destination:
            host: productpage
            subset: 1.1-SNAPSHOT
          weight: "45"
