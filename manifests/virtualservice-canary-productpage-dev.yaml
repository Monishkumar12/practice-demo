apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: productpage-dev
spec:
  hosts:
    - dev
  http:
    - route:
        - destination:
            host: dev
            subset: dev
          weight: 100
